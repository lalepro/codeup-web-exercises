'use strict';

var input

do {
        input = prompt("# must be between 1 & 50!!!!");

} while(input > 50 || input < 1 || isNaN(parseFloat(input)));

 console.log("Number to skip is " + input);
for(var i = i; i <= 50; i++) {
    continue;
}
if(i == input) {
    console.log("Yikes! Skipping the number " + i);
    continue;
}

console.log("Here is ")
